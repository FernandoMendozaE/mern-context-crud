=========== BACKEND ==================
// Subir imagenes
npm i express-fileupload

// Crear y manipular archivos
npm i fs-extra

// Enviar mensajes de error al cliente 
npm i http-errors 

// Para subir imagenes al servicio cloudinary
npm i cloudinary

// Intalaciones módulos
npm i express express-fileupload mongoose dotenv fs-extra http-errors morgan 

npm i nodemon -D

============== FRONTEND =================
npm i react-router-dom react-hot-toast react-icons axios formik yup 

// formik => recibir el estado, mensajes de errores
// yup => validar formularios (string, int)